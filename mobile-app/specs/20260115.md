Implement the app. Some points:

1. The `prototypes/money-coach-v1-backend-spec.md` states the tech details for the backend (with implementation gaps recorded in `app-backend/docs/implementation-gaps.md`), and the doc `prototypes/money-coach-v1-prototypes.md` specifies the details for the app, see `specs/PRD.md` for a marketing-styled broader context, and see `specs/Money Coach AI 1.0完整策略库技术实现方案.md` for more descriptions of the strategies.

2. The tests in the backend directory demonstrate some core flows. Refer to them if you find helpful.

3. Add in sufficient tests in the app directory.

4. Test the app against localhost (spin up the backend using Docker, and remove the volumes to reset database when necessary) for now. The backend will be deployed to `api.moneycoach.cc` in the future. View `app-backend/.env` to check the environment variables configured for local development.

5. Add CI code for the app directory.

6. You may also modify the backend directory when necessary.