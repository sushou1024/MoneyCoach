Implement the app-backend. Some points:

1. Follow `prototypes/money-coach-v1-backend-spec.md` for technical details, refer to `prototypes/money-coach-v1-prototypes.md` for how the endpoints will be used, see `specs/PRD.md` for a marketing-styled broader context, and see `specs/Money Coach AI 1.0完整策略库技术实现方案.md` for more descriptions of the strategies.

2. The following env vars are already set `.env`:

```
GEMINI_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
CMC_PRO_API_KEY=
COINGECKO_PRO_API_KEY=
MARKETSTACK_ACCESS_KEY=
OPEN_EXCHANGE_APP_ID=
BINANCE_API_BASE_URL=https://api.binance.com
```

3. Unit tests & integration tests are already implemented. Required data sources and LLM invokations for MVP should have all been covered in the existing tests. I've run `DEBUG_TEST_OUTPUT=1 go test -timeout 1h -v ./... > all.log` and all the tests have passed. Review the log yourself if necessary.

4. Write an implementation gap doc to record the intentional drifts from the docs if you find necessary.
